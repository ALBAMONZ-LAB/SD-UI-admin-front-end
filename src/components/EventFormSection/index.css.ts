import { style } from "@vanilla-extract/css";

export const eventFormSection = style({
  display: 'flex',
  flexDirection: 'column',
});

export const eventFormMain = style({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
});